{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "polar-token-list",
  "version": "0.1.0",
  "private": true,
  "packageManager": "yarn@4.5.2",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "index": "node src/index.ts",
    "compose": "yarn build && node dist/index.js",
    "discover": "yarn build && node dist/scripts/discover.js"
  },
  "dependencies": {
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
