name: Generate Token Lists

on:
  push:
    branches: [main, develop]
    paths:
      - 'data/strict-tokens.json'
      - 'data/banned.json'
      - 'data/verified-tokens.json'
      - 'data/all-verified-tokens.json'
  pull_request:
    paths:
      - 'data/strict-tokens.json' 
      - 'data/banned.json'
      - 'data/verified-tokens.json'
      - 'data/all-verified-tokens.json'

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Enable Corepack
        run: corepack enable

      - name: Use Yarn 4.5.2
        run: corepack prepare yarn@4.5.2 --activate

      - name: Cache Yarn
        uses: actions/cache@v4
        with:
          path: |
            .yarn/cache
            .yarn/install-state.gz
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: corepack yarn install --immutable

      - name: Build project
        run: corepack yarn build

      - name: Validate token data
        run: corepack yarn validate

      - name: Generate token lists
        run: corepack yarn generate

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet dist/; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Show generated files info
        if: steps.changes.outputs.changed == 'true'
        run: |
          echo "📊 Generated token lists:"
          echo "- all.json: $(jq '.tokens | length' dist/all.json) tokens"
          echo "- strict.json: $(jq '.tokens | length' dist/strict.json) tokens"
          echo "- banned.json: $(jq '.banned | length' dist/banned.json) banned tokens"

      - name: Commit generated files
        if: steps.changes.outputs.changed == 'true' && github.event_name == 'push'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add dist/
          git commit -m "🤖 Auto-generate token lists

          - all.json: $(jq '.tokens | length' dist/all.json) tokens
          - strict.json: $(jq '.tokens | length' dist/strict.json) tokens  
          - banned.json: $(jq '.banned | length' dist/banned.json) banned tokens
          
          Generated from commit: ${{ github.sha }}"

      - name: Push changes
        if: steps.changes.outputs.changed == 'true' && github.event_name == 'push'
        run: git push

      - name: Comment on PR
        if: steps.changes.outputs.changed == 'true' && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const allTokens = JSON.parse(fs.readFileSync('dist/all.json', 'utf8'));
            const strictTokens = JSON.parse(fs.readFileSync('dist/strict.json', 'utf8'));
            const bannedTokens = JSON.parse(fs.readFileSync('dist/banned.json', 'utf8'));
            
            const comment = `## 🤖 Token Lists Generated
            
            | List | Count | Status |
            |------|-------|--------|
            | All Tokens | ${allTokens.tokens.length} | ✅ |
            | Strict Tokens | ${strictTokens.tokens.length} | ✅ |
            | Banned Tokens | ${bannedTokens.banned.length} | ✅ |
            
            **Updated:** ${new Date().toISOString()}
            
            The token lists have been automatically generated and are ready for review.`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: comment
            });